# Deployment Flow Diagram

## 🔄 Complete Deployment Process

```
┌─────────────────────────────────────────────────────────────────┐
│                    DEPLOYMENT ORCHESTRATION                      │
└─────────────���───────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  Step 1: PRE-DEPLOYMENT VALIDATION                              │
│  Script: ./pre-deploy-check.sh                                  │
├─────────────────────────────────────────────────────────────────┤
│  ✓ Check Azure CLI                                              │
│  ✓ Check Terraform                                              │
│  ✓ Check kubectl                                                │
│  ✓ Check Maven                                                  │
│  ✓ Check Docker                                                 │
│  ✓ Verify Azure authentication                                  │
│  ✓ Verify remote backend                                        │
│  ✓ Validate project files                                       │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 2: BUILD APPLICATION                                      │
│  Command: mvn clean package -DskipTests                         │
├─────────────────────────────────────────────────────────────────┤
│  → Compile Java source code                                     │
│  → Run unit tests (optional)                                    │
│  → Package into JAR file                                        │
│  Output: target/kafkaStudy-0.0.1-SNAPSHOT.jar                  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 3: BUILD DOCKER IMAGE                                     │
│  Command: docker build -t kafka-study-app:dev-latest .         │
├─────────────────────────────────────────────────────────────────┤
│  → Use OpenJDK 22 base image                                    │
│  → Copy JAR file to container                                   │
│  → Set up entry point                                           │
│  Output: kafka-study-app:dev-latest                            │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 4: PROVISION INFRASTRUCTURE (Terraform)                   │
│  Script: ./deploy-infrastructure.sh                             │
├─────────────────────────────────────────────────────────────────┤
│  4.1 Initialize Terraform                                       │
│      → Connect to Azure remote backend                          │
│      → Download providers (azurerm)                             │
│      → Initialize state file                                    │
│                                                                 │
│  4.2 Validate Configuration                                     │
│      → Check syntax                                             │
│      → Verify resource definitions                              │
│                                                                 │
│  4.3 Plan Infrastructure Changes                                │
│      → Calculate resource changes                               │
│      → Show what will be created                                │
│      → Generate execution plan                                  │
│                                                                 │
│  4.4 Apply Infrastructure                                       │
│      → Create Resource Group                                    │
│      → Create Virtual Network (10.0.0.0/16)                    │
│      → Create Subnet (10.0.1.0/24)                             │
│      → Create AKS Cluster (2 nodes)                            │
│      → Configure networking                                     │
│      → Enable RBAC                                              │
│                                                                 │
│  4.5 Get AKS Credentials                                        │
│      → Fetch kubeconfig                                         │
│      → Configure kubectl context                                │
│                                                                 │
│  Duration: ~10-15 minutes                                       │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌────────────────────────────────────────────────��────────────────┐
│  Step 5: DEPLOY KUBERNETES RESOURCES                            │
│  Script: ./deploy-application.sh                                │
├─────────────────────────────────────────────────────────────────┤
│  5.1 Verify kubectl Connection                                  │
│      → Check cluster connectivity                               │
│      → Verify current context                                   │
│                                                                 │
│  5.2 Create Namespace                                           │
│      → kubectl create namespace dev                             │
│                                                                 │
│  5.3 Build Kustomize Configuration                              │
│      → Load base manifests                                      │
│      → Apply dev overlay                                        │
│      → Generate final YAML                                      │
│                                                                 │
│  5.4 Deploy Resources (in order)                                │
│                                                                 │
│      5.4.1 MongoDB StatefulSet                                  │
│            → Secret (credentials)                               │
│            → PVC (1Gi storage)                                  │
│            → Service (ClusterIP: None)                          │
│            → StatefulSet (1 replica)                            │
│            → Init containers for sysctl                         │
│                                                                 │
│      5.4.2 Kafka StatefulSet                                    │
│            → ConfigMap (broker config)                          │
│            → PVC (5Gi storage)                                  │
│            → Service (ClusterIP: None)                          │
│            → StatefulSet (1 replica)                            │
│                                                                 │
│      5.4.3 Application Deployment                               │
│            → ConfigMap (app config)                             │
│            → Secret (MongoDB URI)                               │
│            → Service (ClusterIP)                                │
│            → Deployment (1 replica)                             │
│                                                                 │
│  5.5 Wait for Pods to be Ready                                  │
│      → Monitor pod status                                       │
│      → Wait up to 5 minutes                                     │
│      → Check health                                             │
│                                                                 │
│  Duration: ~5-10 minutes                                        │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  Step 6: POST-DEPLOYMENT VERIFICATION                           │
├─────────────────────────────────────────────────────────────────┤
│  → Display all resources (kubectl get all -n dev)               │
│  → Show service endpoints                                       │
│  → Verify pod status                                            │
│  → Check PVC binding                                            │
│  → Display logs (optional)                                      │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    ✅ DEPLOYMENT COMPLETE                        │
└─────────────────────────────────────────────────────────────────┘
```

## 🔁 CI/CD Pipeline Flow (GitHub Actions)

```
┌─────────────────────────────────────────────────────────────────┐
│  TRIGGER: Push to main/develop OR Manual trigger                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  JOB 1: BUILD AND TEST                                          │
├─────────────────────────────────────────────────────────────────┤
│  → Checkout code                                                │
│  → Setup JDK 21                                                 │
│  → Run tests (mvn test)                                         │
│  → Build JAR (mvn package)                                      │
│  → Upload artifact                                              │
│  Duration: ~3-5 minutes                                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  JOB 2: BUILD DOCKER IMAGE                                      │
├─────────────────────────────────────────────────────────────────┤
│  → Checkout code                                                │
│  → Download JAR artifact                                        │
│  → Setup Docker Buildx                                          │
│  → Login to Azure Container Registry                            │
│  → Build Docker image                                           │
│  → Push to ACR with tags:                                       │
│    - <commit-sha>                                               │
│    - dev-latest                                                 │
│  Duration: ~2-3 minutes                                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌──────���──────────────────────────────────────────────────────────┐
│  JOB 3: DEPLOY INFRASTRUCTURE                                   │
├─────────────────────────────────────────────────────────────────┤
│  → Checkout code                                                │
│  → Azure Login (Service Principal)                              │
│  → Setup Terraform                                              │
│  → Terraform Init (with backend config)                         │
│  → Terraform Validate                                           │
│  → Terraform Plan                                               │
│  → Terraform Apply (auto-approve)                               │
│  → Get AKS credentials                                          │
│  Duration: ~10-12 minutes                                       │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  JOB 4: DEPLOY APPLICATION                                      │
├─────────────────────────────────────────────────────────────────┤
│  → Checkout code                                                │
│  → Azure Login                                                  │
│  → Get AKS credentials                                          │
│  → Create namespace (dev)                                       │
│  → Update image tags in kustomization                           │
│  → Apply Kubernetes manifests (kubectl apply -k)                │
│  → Wait for pods to be ready                                    │
│  → Display deployment status                                    │
│  Duration: ~5-7 minutes                                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  JOB 5: HEALTH CHECK                                            │
├─────────────────────────────────────────────────────────────────┤
│  → Azure Login                                                  │
│  → Get AKS credentials                                          │
│  → Check pod health                                             │
│  → Verify all pods running                                      │
│  → Display final status                                         │
│  Duration: ~1-2 minutes                                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│              ✅ CI/CD PIPELINE COMPLETE                          │
│              Total Duration: ~22-30 minutes                     │
└─────────────────────────────────────────────────────────────────┘
```

## 🔧 Resource Creation Order

```
1. TERRAFORM RESOURCES
   └─ Resource Group: rg-kafka-study-dev
      └─ Virtual Network: aks-kafka-study-dev-vnet (10.0.0.0/16)
         └─ Subnet: aks-subnet (10.0.1.0/24)
            └─ AKS Cluster: aks-kafka-study-dev
               ├─ Node Pool: system (2 nodes)
               ├─ VM Size: Standard_DS2_v2
               ├─ Network: Azure CNI
               └─ RBAC: Enabled

2. KUBERNETES RESOURCES (namespace: dev)
   
   Step 1: Storage
   ├─ PVC: dev-kafka-data (5Gi)
   └─ PVC: dev-mongo-storage (1Gi)
   
   Step 2: Secrets & ConfigMaps
   ├─ Secret: dev-mongo-secret (credentials)
   ├─ Secret: dev-my-secret (MongoDB URI)
   ├─ ConfigMap: dev-kafka-config (broker config)
   ├─ ConfigMap: dev-my-config (app config)
   └─ ConfigMap: dev-env-config (environment)
   
   Step 3: StatefulSets
   ├─ StatefulSet: dev-mongodb-sfs
   │  ├─ Replicas: 1
   │  ├─ Service: dev-mongodb-service (27017)
   │  └─ Volume: mongo-storage (1Gi)
   │
   └─ StatefulSet: dev-kafka-sfs
      ├─ Replicas: 1
      ├─ Service: dev-kafka-service (9092, 9093)
      └─ Volume: kafka-data (5Gi)
   
   Step 4: Deployments
   └─ Deployment: dev-my-app
      ├─ Replicas: 1
      ├─ Service: dev-app (8080)
      └─ Image: kafka-study-app:dev-latest
```

## 🛣️ Network Flow

```
External Access (via port-forward)
           ↓
    kubectl port-forward
           ↓
┌──────────────────────────┐
│   dev-app:8080           │ ← Application Service
│   (ClusterIP)            │
└──────────┬───────────────┘
           │
           ↓
┌──────────────────────────┐
│   dev-my-app             │ ← Application Pod
│   Container: app         │
└──────────┬───────────────┘
           │
           ├─────────────────────────────┐
           │                             │
           ▼                             ▼
┌──────────────────────┐    ┌──────────────────────┐
│ dev-kafka-service    │    │ dev-mongodb-service  │
│ (Headless)           │    │ (Headless)           │
│ Port: 9092           │    │ Port: 27017          │
└──────────┬───────────┘    └──────────┬───────────┘
           │                           │
           ▼                           ▼
┌──────────────────────┐    ┌──────────────────────┐
│ dev-kafka-sfs-0      │    │ dev-mongodb-sfs-0    │
│ Volume: 5Gi          │    │ Volume: 1Gi          │
└──────────────────────┘    └──────────────────────┘

DNS Resolution:
- kafka-service.dev.svc.cluster.local:9092
- mongodb-service.dev.svc.cluster.local:27017
```

## 🔄 Rollback Flow

```
┌─────────────────────────────────────────────────────────────────┐
│  ROLLBACK SCENARIO: Deployment Issues                           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  Run: ./rollback.sh                                             │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  Choose Rollback Option:                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Option 1: Rollback to Previous Revision (Recommended)          │
│  ─────────────────────────────────────────────────────          │
│  → kubectl rollout undo deployment/dev-my-app -n dev           │
│  → Kubernetes automatically reverts to previous version         │
│  → Fast (< 1 minute)                                            │
│                                                                 │
│  Option 2: Rollback to Specific Revision                        │
│  ────────────────────────────────────────                       │
│  → View revision history                                        │
│  → Select specific revision number                              │
│  → kubectl rollout undo --to-revision=N                         │
│  → Fast (< 1 minute)                                            │
│                                                                 │
│  Option 3: Clean Deployment (Nuclear Option)                    │
│  ───────────────────────────────────────────                    │
│  → Delete all resources in namespace                            │
│  → Delete PVCs (DATA LOSS!)                                     │
│  → Redeploy from scratch                                        │
│  → Slow (~10 minutes)                                           │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 📊 State Management

```
┌─────────────────────────────────────────────────────────────────┐
│                    TERRAFORM STATE                              │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  Azure Storage Account: remotebackendstoragetf                  │
│  Resource Group: rg-terraform_remote_backend                    │
│  Container: terraform-remotebackendfiles                        │
│  State File: KAFKA_AKS_DEV.tfstate                             │
├─────────────────────────────────────────────────────────────────┤
│  Features:                                                      │
│  ✓ Remote storage (shared across team)                         │
│  ✓ State locking (prevents concurrent modifications)           │
│  ✓ Encryption at rest                                          │
│  ✓ Version history                                             │
│  ✓ Backup and recovery                                         │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                  KUBERNETES STATE                               │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│  AKS etcd Database                                              │
│  (Managed by Azure)                                             │
├─────────────────────────────────────────────────────────────────┤
│  Stores:                                                        │
│  → All Kubernetes resource definitions                          │
│  → ConfigMaps and Secrets                                       │
│  → Deployment history (revisions)                               │
│  → Service endpoints                                            │
│  → Pod states                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 🎯 Deployment Decision Tree

```
START: Ready to Deploy?
        │
        ▼
    First time? ────Yes───→ Run pre-deploy-check.sh
        │                          │
        No                         ▼
        │                    All checks pass? ─No─→ Fix issues
        │                          │
        │                         Yes
        │                          │
        ▼◄─────────────────────────┘
Infrastructure exists? ─No─→ Run deploy-infrastructure.sh
        │                          │
       Yes                         │
        │◄─────────────────────────┘
        ▼
Need full deploy? ──Yes──→ Run deploy-all.sh
        │                          │
        No                         ▼
        │                      ✅ Done
        ▼
Just update app? ──Yes──→ Run deploy-application.sh
        │                          │
        No                         ▼
        │                      ✅ Done
        ▼
    Use CI/CD? ──Yes──→ Push to GitHub
        │                          │
        No                         ▼
        │                  GitHub Actions runs
        ▼                          │
  Manual kubectl                   ▼
    commands                   ✅ Done
        │
        ▼
    ✅ Done
```

---

This flow diagram provides a complete visual reference for understanding the deployment process from start to finish!
